name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      pages: write  # Permission for GitHub Pages
      id-token: write  # Permission to authenticate with GitHub OIDC

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4

      - name: Install dependencies
        run: npm ci --omit=optional

      - name: Build project
        run: npm run build

      - name: List all files with sizes
        run: |
          find ./docs -type f -exec du -h {} +

      - name: Upload build artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: ./docs  # Ensure you're uploading the correct build folder

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write

    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4  # Use the correct deploy version